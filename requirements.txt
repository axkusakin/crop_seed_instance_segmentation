# Tested and pinned together on Python 3.11 (2026-08).
# IMPORTANT: numpy must stay <2.0 -- tensorflow==2.15.0 was compiled against
# numpy 1.x ABI and crashes on import under numpy>=2 (ml_dtypes/_ARRAY_API error).
# Installing extra packages without this pin present can silently upgrade numpy
# and break tensorflow; keep this pin whenever adding/upgrading dependencies.
numpy==1.26.4
tensorflow==2.15.0
keras==2.15.0
scipy==1.17.1
Pillow==12.3.0
cython==3.2.9
matplotlib==3.11.1
scikit-image==0.26.0
opencv-python==4.11.0.86
h5py==3.16.0
IPython[all]
protobuf==4.25.9
pandas==3.0.5
scikit-learn==1.9.0
seaborn==0.13.2
pyefd==1.8.0
gdown==6.1.0
# imgaug is only needed if you call mrcnn.model.load_image_gt(..., augmentation=...)
# for training-time data augmentation. grain_metrics.py and the notebook's actual
# inference/evaluation calls never pass `augmentation`, so it is not a hard
# runtime dependency here. Uncomment and pin a version if you add training code.
# imgaug==0.4.0
